import { CategorieTostringPipe } from './categorie-tostring.pipe';

describe('CategorieTostringPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieTostringPipe();
    expect(pipe).toBeTruthy();
  });
});
